{
  "name": "@frontline/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Request schemas (Zod) of the callable functions, shared by the web app and the functions.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@frontline/core": "workspace:*"
  }
}
