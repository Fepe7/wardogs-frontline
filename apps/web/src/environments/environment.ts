/** Build settings. The Firebase projects are added when the demo is deployed. */
export const environment = {
  /** Dev demo: the war runs on bots and simulated matches (docs/DISEÑO.md §5). */
  demoMode: true,
} as const;
