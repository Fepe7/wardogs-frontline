{
  "name": "@frontline/functions",
  "version": "0.0.0",
  "private": true,
  "description": "Cloud Functions of Wardogs Frontline: entry points and infrastructure adapters.",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test:integration": "firebase emulators:exec --only firestore,auth --project demo-frontline \"vitest run\""
  },
  "dependencies": {
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2"
  }
}
