{
  "name": "@frontline/firebase",
  "version": "0.0.0",
  "private": true,
  "description": "Firestore and Storage security rules, with their tests against the emulators.",
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore,storage --project demo-frontline \"vitest run\"",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.13.6"
  }
}
