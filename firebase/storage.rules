rules_version = '2';

// Closed until the match report feature defines its upload path and limits.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
